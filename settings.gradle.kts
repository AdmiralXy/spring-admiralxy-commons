rootProject.name = "commons"

include("${rootProject.name}-domain-core")
include("${rootProject.name}-domain-persistence")
include("${rootProject.name}-messages")
include("${rootProject.name}-validation")
include("${rootProject.name}-exception")
include("${rootProject.name}-cqrs")
